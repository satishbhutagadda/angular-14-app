import { AppTestDirective } from './app-test.directive';

describe('AppTestDirective', () => {
  it('should create an instance', () => {
    const directive = new AppTestDirective();
    expect(directive).toBeTruthy();
  });
});
