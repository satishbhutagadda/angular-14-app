import { RemoveTabSpaceDirective } from './remove-tab-space.directive';

describe('RemoveTabSpaceDirective', () => {
  it('should create an instance', () => {
    const directive = new RemoveTabSpaceDirective();
    expect(directive).toBeTruthy();
  });
});
